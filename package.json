{
  "name": "cv-template",
  "version": "1.0.0",
  "description": "My curriculum vitae template",
  "author": "Markus Hsi-Yang Fritz",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "docs": "cross-env BASE_URL=/cv-template/ npm run build && rimraf docs && ncp dist docs"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "js-yaml-loader": "^1.0.1",
    "minireset.css": "0.0.4",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "ncp": "^2.0.0",
    "nodemon": "^1.18.9",
    "rimraf": "^2.6.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run docs"
    }
  }
}
